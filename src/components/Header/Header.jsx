import React from "react";
import { hot } from "react-hot-loader/root";
import "./Header.scss";

const Header = () => {
  return (
    <div className="mid">
      <span>fsdf</span>
    </div>
  );
};

export default hot(Header);
